#include <stdio.h>

/**
 * main - print the name of the file
 *
 * Description: Print the current file name
 *
 * Return: always 0 if succes
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}

#include <ctype.h>

/**
 * _islower - check for lowercase
 *
 * Description: Return 1 in lowercase else Return 0
 *
 * Return: Always 0 (success)
 */
int _islower(int c)
{
int c = 'a';
while (islower('c'))
{
return (1);
}
return (0);
}
